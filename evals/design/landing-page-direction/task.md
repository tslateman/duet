Apply the /design skill. Design the landing page for our product.

The product is a command-line tool called `seam` that finds dead code across a
monorepo. Users are staff engineers at companies with 500k+ line codebases.
They are skeptical of marketing, they will read the docs before the pitch, and
they mostly find tools through a colleague's link in Slack.

Give me the design direction and the page. Single page, no framework, plain
HTML and CSS.
